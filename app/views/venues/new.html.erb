<div class="container mt-5">
  <div class="row justify-content-center">
    <div class="col-md-6">
      <h2 class="mb-4">New Venue</h2>

      <%= form_with(model: @venue, local: true) do |form| %>
        <div class="mb-3">
          <%= form.label :name, class: "form-label fw-bold" %>
          <%= form.text_field :name, class: "form-control", required: true %>
        </div>

        <div class="mb-3">
          <%= form.label :location, class: "form-label fw-bold" %>
          <%= form.text_field :location, class: "form-control" %>
        </div>

        <div class="mb-4">
          <%= form.label :capacity, class: "form-label fw-bold" %>
          <%= form.number_field :capacity, class: "form-control", required: true %>
        </div>

        <div class="d-flex justify-content-between">
          <%= link_to "Back", venues_path, class: "btn btn-outline-secondary" %>
          <%= form.submit "Create Venue", class: "btn btn-primary" %>
        </div>
      <% end %>
    </div>
  </div>
</div>