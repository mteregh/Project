<div class="container mt-4">
  <h1>Users</h1>

  <ul class="list-group">
    <% @users.each do |user| %>
      <li class="list-group-item">
        <%= link_to user.name, user_path(user) %>
      </li>
    <% end %>
  </ul>
</div>